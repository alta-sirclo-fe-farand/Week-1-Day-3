// Factor

// input
let bilangan = 20;

// process
for (let i = 1; i <= bilangan; i++) {
    if (bilangan % i == 0) {
        console.log(i)
    }
}

// Factor

// input
let bilangan = 20;

// process
for (let i = bilangan; i >= 1; i--) {
    if (bilangan % i == 0) {
        console.log(i)
    }
}